# -*- coding: utf-8 -*-
# vim: ft=yaml
#
# Set default values.
---
values:
  pkg:
    name: proxmox
  rootgroup: root
  config: '/etc/proxmox'
  service:
    name: proxmox
  subcomponent:
    config: '/etc/proxmox-subcomponent-formula.conf'
  # Just here for testing
  added_in_defaults: defaults_value
  winner: defaults
...
